<!--
Modification de la vue 
**********************
On indique au formulaire qu'il doit envoyer ses données à l'URL actuelle, puis on indique au formulaire de se préparer, ce qui déclenche 
quelques éléments internes. Les formulaires HTML peuvent être envoyés en utilisant "POST" et "GET". La valeur par défaut de ZF2 est "POST", 
on n'a donc pas besoin d'être explicite dans la définition de cette option. Si on veut le changer en "GET", tout ce qu'on à à faire, c'est de 
définir l'attribut spécifique avant l'appel de "prepare()". Ensuite, on utilise quelques "View Helpers" qui s'occupent de rentre le formulaire.
Il existe de nombreuses façons de créer un formulaire dans Zend mais l'utilisation de "Form Collection()" est la plus rapide. Le formulaire s'affiche 
mais on n'a pas encore ajouté de logique au contrôleur.
-->
<h1>Ajout de sujets</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url());
$form->prepare();

echo $this->form()->openTag($form);
echo $this->formCollection($form);
echo $this->form()->closeTag();


